workflows:
  fvm-workflow:
    name: FVM workflow
    scripts:
      - name: Install FVM
        script: |
          brew tap leoafarias/fvm  
          brew install fvm
          fvm install 3.41.4 
          fvm use 3.41.4 
      - name: Install dependencies
        script: |
          fvm flutter pub get